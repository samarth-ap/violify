
  # Violify App UI Design

  This is a code bundle for Violify App UI Design. The original project is available at https://www.figma.com/design/4l77AQhp7qQwo0GiydzkNX/Violify-App-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  